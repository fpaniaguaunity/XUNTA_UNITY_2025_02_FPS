using UnityEngine;

public class CambiadorMaterialEsfera : MonoBehaviour
{
    public Material material;
    void Start()
    {
        //La siguiente línea cambia el color del material
        //material.SetColor("_BaseColor", new Color(1, 0, 0));
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
